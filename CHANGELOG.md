# 更新日志

本文件记录热搜交易所面向用户的版本变化。版本号与 `metadata.yaml` 保持一致。

## 0.4.0 - 2026-07-30

### 新增

- 新增可在 WebUI 配置的 `delist_alert_enabled` 持仓股离榜预警，默认启用。
- 行情、详情、资产、排行或状态查询时检查离榜持仓，并使用 AstrBot 的 `Comp.At` 提醒持仓成员。
- 同一群的同一股票在一次离榜周期内只发送一条消息，多位持仓成员合并在该消息中统一 @。
- 预警去重记录持久化到 SQLite；股票重新上榜后自动重置，未来再次离榜可重新提醒。

### 调整

- 退市前的离榜价格衰减由每轮 25% 下调为每轮 3%。

### 兼容性

- 启动时自动创建 `delist_alerts` 表，无需手动迁移现有数据库。
- 已有持仓、订单、账户、配置和股票代码继续有效。

## 0.3.0 - 2026-07-30

### 新增

- `/热市 详情` 在启用图片渲染时输出独立个股行情卡片，展示近 24 轮走势图、区间高低点、区间涨跌和快捷买入指令。
- 行情定价加入温和多头偏置，以约 60% 上涨家数为软目标。
- 同榜或升榜股票获得受限的微幅正向漂移，最多达到排名目标价上方 2%。
- 资产查询金额达到 1 万热币时自动显示为“万”单位。

### 调整

- jieba TF-IDF 股票关键词从最长 8 个字符缩短到 5 个字符；冲突后缀和旧代码别名机制保持不变。
- 资产排行与每日大模型复盘只统计至少有一笔成交订单的成员，查看资产但未交易的账户不再进入榜单。
- 最低 AstrBot 版本声明为 `4.5.7`，以匹配默认大模型复盘所使用的插件 API。

### 兼容性

- 无需迁移现有 SQLite 数据库。
- 已有持仓、订单、账户、WebUI 配置和旧股票代码继续有效。

## 0.2.0 - 2026-07-30

- 使用 jieba TF-IDF 生成可读股票代码，并加入稳定冲突后缀与旧代码别名。
- 丰富行情图的盘面情绪、标语、热度王、波动王和快捷买入提示。
- 新增按群会话默认大模型生成的定时每日持仓复盘。

## 0.1.1 - 2026-07-30

- 修复买入金额为字符串时触发 `type str doesn't define __round__ method` 的问题。

## 0.1.0 - 2026-07-30

- 发布多平台独立热搜股市 MVP。
- 支持行情、详情、买入、卖出、资产、排行、状态和刷新指令。
